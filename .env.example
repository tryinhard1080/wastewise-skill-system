# WasteWise Skill System - Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# Supabase Configuration
# =============================================================================

# Public Supabase URL (safe to expose to clients)
# Get from: Supabase Dashboard > Project Settings > API
# Local development: http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Public Anonymous Key (safe to expose to clients)
# Get from: Supabase Dashboard > Project Settings > API > Project API keys > anon public
# Local development: Run `npx supabase start` to get this value
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Service Role Key (KEEP SECRET! Never expose to client)
# Used by: Background workers, server-side operations, RLS bypass
# Get from: Supabase Dashboard > Project Settings > API > Project API keys > service_role
# Local development: Run `npx supabase start` to get this value
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# =============================================================================
# Worker Configuration (Optional)
# =============================================================================

# How often the worker checks for new jobs (in milliseconds)
# Default: 5000 (5 seconds)
# Recommended: 5000-10000 for production
WORKER_POLL_INTERVAL_MS=5000

# Maximum number of jobs to process concurrently
# Default: 1
# Recommended: 1-3 for single-instance workers
WORKER_CONCURRENCY=1

# =============================================================================
# AI Configuration (Phase 2.2+)
# =============================================================================

# Anthropic API Key (for invoice extraction, regulatory research)
# Get from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=your_api_key_here

# =============================================================================
# Application Configuration
# =============================================================================

# Application environment
# Options: development, staging, production
NODE_ENV=development

# Base URL for the application (used for absolute URLs)
# Local: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Logging & Observability (Optional)
# =============================================================================

# Log level: debug, info, warn, error
# Default: info
LOG_LEVEL=info

# =============================================================================
# Local Development Notes
# =============================================================================
#
# 1. Start Supabase locally:
#    npx supabase start
#
# 2. This will output connection details including:
#    - API URL (use for NEXT_PUBLIC_SUPABASE_URL)
#    - anon key (use for NEXT_PUBLIC_SUPABASE_ANON_KEY)
#    - service_role key (use for SUPABASE_SERVICE_ROLE_KEY)
#
# 3. Example local values after running `npx supabase start`:
#    NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
#    NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
#    SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
#
# =============================================================================
